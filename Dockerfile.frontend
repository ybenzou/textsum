# 构建阶段
FROM node:20 AS builder

WORKDIR /app
COPY frontend/ ./
RUN npm install -g pnpm && pnpm install && pnpm run build

# 运行阶段（只用 Nginx 提供静态文件）
FROM nginx:stable-alpine AS production

COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
